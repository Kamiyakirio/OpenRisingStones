{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "fishing-timer",
  "description": "Local fishing timer events and title-bar dragging only.",
  "windows": ["fishing-timer"],
  "permissions": ["core:event:default", "core:window:allow-start-dragging"]
}
