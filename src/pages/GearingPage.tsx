/** The host owns navigation and theme; gearing owns only its desktop editing workspace. */
import { Editor } from "../features/gearing/editor/Editor";
export function GearingPage() {
  return <Editor />;
}
